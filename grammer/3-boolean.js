var c ;

console.log((c== undefined))    // undefined behaves as false

var myArray = [];

console.log(myArray == undefined)   // it will return false because myArray already decalred

console.log(!myArray[0])    // myArray[0] not exist so it will return true


